{
  "name": "multi-agent-flow",
  "version": "0.0.0",
  "private": true,
  "description": "A lightweight role- and skill-constrained multi-agent workflow platform",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @maf/web dev",
    "build": "pnpm --filter @maf/web build",
    "lint": "pnpm --filter @maf/web lint",
    "test": "pnpm --filter @maf/web test"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
