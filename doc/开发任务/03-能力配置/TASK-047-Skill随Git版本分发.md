# TASK-047 Skill 随 Git 版本分发
## 前置条件
- TASK-011、TASK-045 已完成。
## 任务内容
- 定义已发布 Skill 在仓库中的索引、内容哈希、只读路径和节点本地校验读取。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-047` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 节点只能读取 Role 绑定的精确版本。
- [x] 文件哈希与索引不符时停止任务。
- [x] 路径不能逃逸仓库 Skill 根目录。
## 不包含
- HTTP Skill 下载接口。
