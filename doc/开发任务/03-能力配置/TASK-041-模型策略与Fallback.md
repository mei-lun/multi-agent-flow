# TASK-041 模型策略与 Fallback
## 前置条件
- TASK-040 已完成。
## 任务内容
- 实现主模型、有序 fallback、重试次数、允许错误类别和不可变版本。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-041` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 策略/预算/权限错误不会触发 fallback。
- [ ] 只有规范化可重试错误切换模型。
- [ ] 已启动任务固定策略版本。
## 不包含
- 节点本地模型映射。
