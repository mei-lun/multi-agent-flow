# TASK-097 Scheduler 回放与故障注入
## 前置条件
- TASK-059～065、TASK-096 已完成。
## 任务内容
- 记录并回放 Git events、Gate 和人工决定，注入 control push 后 DB 失败、重启、重复唤醒和节点失联。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-097` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 相同历史得到相同最终状态。
- [ ] push 成功但投影失败可自动追平。
- [ ] 不重复创建任务、Attempt、Review 或 PR。
## 不包含
- 性能压测。
