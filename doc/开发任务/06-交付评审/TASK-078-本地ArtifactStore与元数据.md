# TASK-078 本地 ArtifactStore 与元数据
## 前置条件
- TASK-006～009 已完成。
## 任务内容
- 实现临时流式写入、长度/哈希复核、内容寻址落盘、Artifact/Version 元数据和引用计数。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-078` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 大文件不一次读入内存。
- [x] 哈希不符不创建版本。
- [x] API 不暴露宿主绝对路径。
## 不包含
- 跨节点 Artifact HTTP；节点交付使用 Git/LFS。
