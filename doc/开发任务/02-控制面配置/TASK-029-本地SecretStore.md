# TASK-029 本地 SecretStore
## 前置条件
- TASK-003～005 已完成。
## 任务内容
- 实现 OS Keyring 优先、AES-GCM 回退的 Secret 创建、解析、轮换和吊销。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-029` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 明文不进入 SQLite、日志、事件或 API 响应。
- [x] 轮换失败保留旧值。
- [x] Keyring 与回退实现有契约测试。
## 不包含
- Vault/KMS。
