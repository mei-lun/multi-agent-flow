# TASK-032 系统设置
## 前置条件
- TASK-029～031 已完成。
## 任务内容
- 实现预定义设置 Schema、读取、乐观锁更新和敏感设置 Secret 引用。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-032` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 未知 key 拒绝。
- [x] 敏感值只返回 configured/fingerprint。
- [x] 并发更新产生版本冲突而非覆盖。
## 不包含
- 节点本地模型设置同步。
