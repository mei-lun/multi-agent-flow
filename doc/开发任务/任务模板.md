# TASK-XXX 任务名称

## 前置条件

- TASK-XXX 已完成。

## 任务内容

- 只描述本任务需要实现的内容、文件和边界。

## 允许改动文件

- `path/to/production.py`：允许实现的生产文件。
- `tests/.../test_xxx.py`：本任务测试。

## 允许新增或修改接口

- `Protocol.method(input) -> output`：说明允许修改的签名和语义。
- 无接口变更时明确写“无”。

## 禁止改动

- 未列出的模块、公共 DTO、API 路径和数据库表。

## 验收标准

- [ ] 可观察、可测试的结果。

## 不包含

- 明确留给其他任务的内容。
