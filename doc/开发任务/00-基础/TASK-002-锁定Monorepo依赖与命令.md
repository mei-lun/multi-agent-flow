# TASK-002 锁定 Monorepo 依赖与命令
## 前置条件
- TASK-001 已完成。
## 任务内容
- 完成 Python、pnpm workspace、格式化、静态检查和测试依赖配置。
- 固定 `bootstrap.ps1`、`dev.ps1`、`verify.ps1` 命令入口。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-002` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 新环境可按 README 安装依赖。
- [x] Python 与 Web 最小入口可执行。
- [x] `verify.ps1` 能运行空测试集和静态检查。
## 不包含
- 任何业务模块。
