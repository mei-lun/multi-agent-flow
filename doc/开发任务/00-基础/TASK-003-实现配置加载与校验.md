# TASK-003 实现配置加载与校验
## 前置条件
- TASK-002 已完成。
## 任务内容
- 实现 Server/Node 配置类型、环境变量映射、路径规范化和启动校验。
- 区分中央数据目录、节点工作区、Git 仓库、本地模型映射和非敏感默认值。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-003` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 缺少必填配置时给出字段级错误并停止启动。
- [ ] Secret 不出现在配置打印和异常中。
- [ ] 相对路径解析结果位于允许根目录。
## 不包含
- Secret 的加密存储。
