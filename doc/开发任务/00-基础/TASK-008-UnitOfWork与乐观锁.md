# TASK-008 UnitOfWork 与乐观锁
## 前置条件
- TASK-006、TASK-007 已完成。
## 任务内容
- 实现异步 UnitOfWork、短写事务、提交/回滚和通用 expected_version 更新辅助函数。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-008` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 未 commit 或发生异常时自动回滚。
- [ ] 并发版本更新只有一个成功，另一个返回冲突。
- [ ] 网络和 Git 操作不会在写事务内执行。
## 不包含
- 具体 Repository SQL。
