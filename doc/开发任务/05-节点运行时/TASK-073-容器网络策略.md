# TASK-073 容器网络策略
## 前置条件
- TASK-050、TASK-071、TASK-072 已完成。
## 任务内容
- 实现离线、域名/端口白名单和获批外网模式，防止 DNS 重绑定与私网访问。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-073` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 默认无外网。
- [ ] loopback、link-local、私网和非法重定向拒绝。
- [ ] 任务结束清理网络规则。
## 不包含
- 商业代理服务。
