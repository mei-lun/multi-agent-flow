# TASK-069 Generic 工作区
## 前置条件
- TASK-003、TASK-068 已完成。
## 任务内容
- 为文档/分析任务创建独立目录，校验输入路径和声明可写子目录。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-069` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 不同任务目录隔离。
- [x] 路径逃逸和链接越界拒绝。
- [x] 失败/结束后可幂等清理。
## 不包含
- Git worktree。
