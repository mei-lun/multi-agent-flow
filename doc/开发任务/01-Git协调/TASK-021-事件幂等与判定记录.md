# TASK-021 事件幂等与判定记录
## 前置条件
- TASK-008、TASK-011、TASK-019 已完成。
## 任务内容
- 按 event_id 保存接受/拒绝决定、reason_code 和 control commit，支持重复消费。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-021` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 同 event_id 相同内容返回首次决定。
- [ ] 同 event_id 不同内容被判冲突。
- [ ] 重启后不会重复应用状态变化。
## 不包含
- 具体事件业务规则。
