# TASK-020 验证节点 Git 身份
## 前置条件
- TASK-013、TASK-019 已完成。
## 任务内容
- 将节点清单中的 Git 身份/签名指纹与事件提交者、分支 node_id 绑定验证。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-020` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 冒用其他 node_id 的事件被拒绝。
- [ ] 未登记或被隔离节点不能 Claim。
- [ ] 拒绝原因写审计且不泄露凭据。
## 不包含
- GPG/SSH 密钥签发服务。
