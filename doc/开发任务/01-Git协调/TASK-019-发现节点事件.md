# TASK-019 发现节点事件
## 前置条件
- TASK-012、TASK-016、TASK-018 已完成。
## 任务内容
- 中央 fetch `maf/node/*`，发现相对已处理水位的新事件并保持可重放顺序。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-019` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 多节点事件都能发现。
- [x] 强推、历史回退和损坏事件分支被隔离。
- [x] 扫描不依赖事件机器时间排序。
## 不包含
- 状态转换。
