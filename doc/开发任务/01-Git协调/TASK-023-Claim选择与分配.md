# TASK-023 Claim 选择与分配
## 前置条件
- TASK-020～022 已完成。
## 任务内容
- 校验 READY、依赖、节点能力/容量，在候选 Claim 中确定性选择并写 assignment。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-023` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 同一输入候选得到相同选择。
- [x] 同一任务只能接受一个当前 owner。
- [x] 接受/拒绝决定都有 reason_code。
## 不包含
- 节点执行任务。
