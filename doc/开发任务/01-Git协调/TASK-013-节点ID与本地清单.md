# TASK-013 节点 ID 与本地清单
## 前置条件
- TASK-003、TASK-011 已完成。
## 任务内容
- 首次运行生成持久随机 node_id，采集 Git 身份、能力、模型别名、Docker Profile 和容量。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-013` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [ ] 重启后 node_id 不变化。
- [ ] 不读取硬件序列号生成身份。
- [ ] 清单通过 node-v1 Schema。
## 不包含
- 将清单提交到远端。
