# TASK-011 加载 MAF 协议与 Schema
## 前置条件
- TASK-001、TASK-002、TASK-005 已完成。
## 任务内容
- 实现 `.maf/project.yaml` 与 task/node/event JSON Schema 加载、版本检查和错误定位。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-011` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 合法模板通过；未知协议版本拒绝。
- [x] 缺字段、额外字段和错误枚举返回文件路径与字段路径。
- [x] Schema 校验有固定样例测试。
## 不包含
- Git fetch/push。
