# TASK-022 任务状态转换引擎
## 前置条件
- TASK-005、TASK-011 已完成。
## 任务内容
- 实现 Task 状态机、允许操作方、版本递增、依赖完成和终态保护。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-022` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 所有允许/禁止转换有参数化测试。
- [x] 节点不能直接设置 DONE、ASSIGNED 或 REVIEWING。
- [x] 终态不会退回 IN_PROGRESS。
## 不包含
- Git 提交。
