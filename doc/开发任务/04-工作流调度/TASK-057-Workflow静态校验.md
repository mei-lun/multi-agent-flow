# TASK-057 Workflow 静态校验
## 前置条件
- TASK-053、TASK-056 已完成。
## 任务内容
- 校验 start、可达性、结束路径、环、Role、输入输出 Contract、受限条件和重试/返工上限。
## 允许改动文件
- 仅限《任务范围清单》中 `TASK-057` 行列出的生产文件和测试文件。
## 允许新增或修改接口
- 仅限该行列出的接口/类型；其他公共接口只允许调用。
## 禁止改动
- 未列出的文件、接口、数据库表、API 路径和 Git Schema。
## 验收标准
- [x] 一次返回全部错误和警告。
- [x] 条件表达式不能执行 Python/JavaScript。
- [x] 不可达节点和无限返工阻断发布。
## 不包含
- Graph 运行。
