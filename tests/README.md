# Tests

- `unit`：纯领域、状态机、图算法和校验器。
- `contract`：模型、Tool、Repository 和 Artifact 契约。
- `integration`：SQLite、LangGraph、Docker、本地 Git。
- `security`：路径穿越、SSRF、越权、Secret 和容器边界。
- `e2e`：网站开发团队从 ProjectBrief 到 PR/merge。
- `fixtures`：测试模板、Skill、仓库和 checkpoint。
