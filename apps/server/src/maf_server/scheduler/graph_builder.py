"""Compile a published workflow version into the LangGraph execution graph."""

from typing import Any


def compile_workflow(workflow_version: object) -> Any:
    """Return a compiled graph; implementation is added with LangGraph wiring."""
    raise NotImplementedError

